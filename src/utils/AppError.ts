export class AppError {
    message: string;

    constructor(message: string) {
        this.message = message;
    } 
}

/* constructor = dentro de uma class ele é executado no momento de forma automatica
ele nos obriga a informa os paramentro que foi colocado dentro do construtor

*/